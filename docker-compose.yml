services:
  phantasia-frontend:
    # image:
    build: 
      context: frontend
      dockerfile: ./Dockerfile
    container_name: phantasia-frontend
    restart: unless-stopped
    ports:
      - 3000:3000
    
volumes:
  db: